/**
 * Description: While there are more than one components, Find the closest weight edge that connects this 
          component to any other component and Add this closest edge to MST if not already added.
 */